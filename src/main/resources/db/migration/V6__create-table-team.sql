CREATE TABLE teams (
                        id SERIAL PRIMARY KEY,
                        team_name VARCHAR(255),
                        first_driver_number INTEGER,
                        second_driver_number INTEGER,
                        team_points INTEGER

    );


